import React from 'react';

const SearchBar = () => {
    return (
        <input
            type="text"
            placeholder="Search..."
            className="block w-full p-2 mb-4 border rounded"
        />
    );
};

export default SearchBar;